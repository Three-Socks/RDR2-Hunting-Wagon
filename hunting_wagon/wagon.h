#pragma once

void wagon_update();
void wagon_vehicle_spawn_action(Hash vehicle_hash, Vector3 spawn_vehicle_coordss, float spawn_vehicle_heading);
void wagon_spawn_vehicle(Hash vehicle_hash, Vector3 spawn_vehicle_coordss, float spawn_vehicle_heading);
